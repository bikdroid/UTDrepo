$(document).ready(function(){
	/*
	Here, we can specify stuff that needs to be there, 
	when the page loads.
	*/
	#(".add_education").click(function(){

		console.log("clicked !!!");
		// $(".all_educations").append("<b>text</b>");
	});
})